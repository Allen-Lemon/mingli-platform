# 复制本文件为 .env 并修改，切勿将含真实密码的 .env 提交到代码仓库
# （.gitignore 已忽略 .env，.env.example 仅作模板保留）

# MySQL root 密码（同时作为 Spring 数据源密码，二者自动保持一致）
# 生产环境务必改为强密码
MYSQL_ROOT_PASSWORD=change_me_to_a_strong_password

# 数据库名（MySQL 初始化建库 + 后端连接串共用，保持一致即可）
MYSQL_DATABASE=mingli
